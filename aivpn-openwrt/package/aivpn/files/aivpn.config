config aivpn main
	option enabled 0
	# Paste your aivpn:// connection key here
	option connection_key ''
	# Or use explicit server + key (if not using connection_key)
	# option server '1.2.3.4:443'
	# option server_key 'base64pubkey=='
