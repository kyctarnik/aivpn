{
  "name": "aivpn-web",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "bun run --cwd server dev & bun run --cwd client dev",
    "build": "bun run --cwd client build && bun run --cwd server build",
    "start": "bun server/src/index.ts",
    "migrate": "bun run --cwd server migrate"
  }
}
