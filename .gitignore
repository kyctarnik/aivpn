# ── Rust ────────────────────────────────────────────────────────────────────
target/
**/target/
*.rs.bk
Cargo.lock

# ── Build output ─────────────────────────────────────────────────────────────
/releases/

# ── R&D sandbox (neural-mask generation prototype; never committed) ───────────
research/

# Windows GUI build staging (make windows)
aivpn-windows-gui-package/
aivpn-windows-gui.zip

# Linux AppImage staging (make linux-appimage)
AppDir-aivpn-linux/

# ── Secrets & keys ──────────────────────────────────────────────────────────
*.pem
*.key
*.secret
secrets/
.env
*.env
.secrets
credentials.json

# ── Server config (keep templates, ignore real configs) ──────────────────────
config/*.json
!config/*.example.json

# ── Local machine config ─────────────────────────────────────────────────────
# Machine-specific NDK / linker paths — never commit
.cargo/

# ── Claude Code ──────────────────────────────────────────────────────────────
.claude/
CLAUDE.md

# ── Android ──────────────────────────────────────────────────────────────────
platforms/android/**/build/
platforms/android/**/bin/
platforms/android/.gradle/
platforms/android/**/.cxx/
platforms/android/**/captures/
platforms/android/**/local.properties
platforms/android/**/local.properties.tmp
platforms/android/keystore.properties
platforms/android/*.jks
*.apk
*.idsig
*.aab
*.ap_
*.dex
*.class
gen/

# ── macOS ────────────────────────────────────────────────────────────────────
platforms/macos/build/
.DS_Store

# ── IDE ──────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── Logs & temp ──────────────────────────────────────────────────────────────
*.log
tmp/
temp/
*.tmp

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/

# ── Compiled objects ─────────────────────────────────────────────────────────
*.o
*.a
*.so
*.dylib
*.dll

# ── Test coverage ────────────────────────────────────────────────────────────
coverage/
*.gcov
*.gcda
*.gcno

# ── Internal docs (not for public distribution) ──────────────────────────────
docs/

# ── Makefile linux-appimage tool cache ───────────────────────────────────────
/build/

# Release signing keystore — NEVER commit
aivpn-android/
*.jks
*.keystore
